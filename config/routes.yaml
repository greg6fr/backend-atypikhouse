# config/routes.yaml
controllers:
  resource:
    path: ../src/Controller/
    namespace: App\Controller
  type: attribute

api_auth_login:
  path: /api/auth/login
  methods: ["POST"]
  defaults:
    _controller: app.controller.authentication:login
